<Window x:Class="PV_app.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:PV_app"
        xmlns:controls="clr-namespace:System.Windows.Controls;assembly=PresentationFramework"
        mc:Ignorable="d"
        Title="Login"
        Height="492"
        Width="300"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        Style="{StaticResource MaterialDesignWindow}">

    <Window.Resources>
        <Style TargetType="TextBox" x:Key="ValidationTextBoxStyle" BasedOn="{StaticResource MaterialDesignOutlinedTextBox}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsUsernameValid}" Value="False">
                    <Setter Property="BorderBrush" Value="Red"/>
                    <Setter Property="Foreground" Value="Red"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="PasswordBox" x:Key="ValidationPasswordBoxStyle" BasedOn="{StaticResource MaterialDesignOutlinedPasswordBox}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsPasswordValid}" Value="False">
                    <Setter Property="BorderBrush" Value="Red"/>
                    <Setter Property="Foreground" Value="Red"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Background="Transparent" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="0,0,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <GroupBox materialDesign:ElevationAssist.Elevation="Dp6"
                  Style="{StaticResource MaterialDesignCardGroupBox}" 
                  Margin="0,0,0,0" 
                  Grid.RowSpan="4" 
                  HorizontalAlignment="Stretch" 
                  VerticalAlignment="Stretch">
            <GroupBox.Header>
                <TextBlock Text="INICIAR SESIÓN"
                           FontSize="20"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center" Foreground="White" />
            </GroupBox.Header>

            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Image Grid.Row="0"
                       Source="/Assets/Logo.png"
                       Height="80"
                       Margin="0,0,0,20"
                       HorizontalAlignment="Center" />

                <StackPanel Grid.Row="1"
                            VerticalAlignment="Top"
                            HorizontalAlignment="Stretch"
                            Margin="0,0,0,10">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <materialDesign:PackIcon Kind="Account"
                                                 Width="20"
                                                 Height="20"
                                                 VerticalAlignment="Center"
                                                 Margin="0,0,8,0" />
                        <Label Content="Usuario"
                               FontSize="14"
                               VerticalAlignment="Center" />
                    </StackPanel>

                    <TextBox Name="UsernameBox"
                             Text="{Binding Username, Mode=TwoWay}" 
                             materialDesign:HintAssist.Hint="Usuario"
                             Style="{StaticResource ValidationTextBoxStyle}"/>

                    <StackPanel Orientation="Horizontal" Margin="0,20,0,8">
                        <materialDesign:PackIcon Kind="FormTextboxPassword"
                                                 Width="20"
                                                 Height="20"
                                                 VerticalAlignment="Center"
                                                 Margin="0,0,8,0" />
                        <Label Content="Contraseña"
                               FontSize="14"
                               VerticalAlignment="Center" />
                    </StackPanel>

                    <PasswordBox Name="PasswordBox"
                                 Margin="0"
                                 HorizontalAlignment="Stretch"
                                 materialDesign:HintAssist.Hint="Contraseña"
                                 Style="{StaticResource ValidationPasswordBoxStyle}"/>
                </StackPanel>

                <StackPanel Grid.Row="2"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Margin="0,10">
                    <TextBlock Name="ErrorMessage"
                               Text="{Binding ErrorMessageText}"
                               Foreground="#ff0033"
                               FontSize="14"
                               HorizontalAlignment="Center"
                               Visibility="{Binding ErrorMessageVisibility}"
                               TextWrapping="Wrap"
                               MaxWidth="250"
                               VerticalAlignment="Center"
                               TextAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Row="3"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Bottom"
                            Margin="0,0,0,0">
                    <Button Click="LoginButton_Click"
                            Height="35"
                            Width="160"
                            materialDesign:ButtonAssist.CornerRadius="10"
                            IsEnabled="{Binding IsLoginEnabled}"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            ToolTip="Iniciar sesión">
                        <TextBlock Text="Iniciar Sesión" FontSize="14" />
                    </Button>
                </StackPanel>
            </Grid>
        </GroupBox>
    </Grid>
</Window>