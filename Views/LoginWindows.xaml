<Window x:Class="PV_app.Views.LoginWindows"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="350" Width="525">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBox Name="UsernameTextBox" Width="300" Margin="0,0,0,10"/>
            <PasswordBox Name="PasswordBox" Width="300" Margin="0,0,0,10"/>
            <Button Name="LoginButton" Content="Login" Width="300" Margin="0,10,0,0" Click="LoginButton_Click"/>
            <TextBlock Text="Forgot Password?" Foreground="Blue" TextAlignment="Center" Margin="0,10,0,0"/>
            <TextBlock Text="Sign Up" Foreground="Blue" TextAlignment="Center" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
    
    <Window.Resources>
    <Style TargetType="Button">
        <Setter Property="Background" Value="#007ACC"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Padding" Value="10"/>
        <Setter Property="Margin" Value="5"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="Cursor" Value="Hand"/>
    </Style>
</Window.Resources>
</Window>
