<Window x:Class="PV_app.Views.ErrorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Error"
        Width="320"
        MinWidth="280"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        Style="{StaticResource MaterialDesignWindow}">

    <Grid Background="Transparent">
        <Border CornerRadius="8"
                Background="White"
                BorderBrush="#B71C1C"
                BorderThickness="2"
                Padding="0"
                Margin="10"
                materialDesign:ElevationAssist.Elevation="Dp4">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="Red" CornerRadius="6,6,0,0">
                    <TextBlock Text="ERROR"
                               FontSize="20"
                               FontWeight="Bold"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Foreground="White"/>
                </Border>

                <StackPanel Grid.Row="1" Margin="15" HorizontalAlignment="Center" MaxWidth="280">
                    <TextBlock x:Name="MessageTextBlock"
                               Text="Error"
                               FontSize="16"
                               TextAlignment="Center"
                               Foreground="Black"
                               TextWrapping="Wrap"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Margin="0,10,0,15" HorizontalAlignment="Center">
                    <Button Content="Cerrar"
                            Click="CloseButton_Click"
                            Width="160"
                            Height="35"
                            Background="Red"
                            BorderBrush="#B71C1C"
                            BorderThickness="2"
                            Foreground="White"
                            FontSize="14"
                            materialDesign:ButtonAssist.CornerRadius="10"
                            Style="{StaticResource MaterialDesignRaisedButton}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
